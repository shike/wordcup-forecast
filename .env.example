# football-data.org API key (optional - leave empty to use Wikipedia only)
FOOTBALL_DATA_API_KEY=

# Use offline mode (no network calls) - true/false
OFFLINE_MODE=false

# Output directory
OUTPUT_DIR=./output

# Cache directory
CACHE_DIR=./cache

# Default language: zh, en, bilingual
DEFAULT_LANG=bilingual
